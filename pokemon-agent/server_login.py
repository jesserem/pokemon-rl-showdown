from poke_env.player_configuration import PlayerConfiguration

def login_showdown(account_name, password=None)
    my_player_config = PlayerConfiguration(account_name, password)
    return my_player_config